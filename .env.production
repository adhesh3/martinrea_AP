# Production config (used by `next build` / `next start`).
# Calls go to the same-origin /api path, which Next rewrites (see next.config.ts)
# to the backend — this avoids browser CORS with the ngrok-free tunnel.
NEXT_PUBLIC_API_BASE_URL=/api
